[push]
	default = matching
[alias]
	st = status
	ci = commit
	br = branch
	co = checkout
	df = diff
	dc = diff --cached
	lg = log -p
	lol = log --pretty=format:'%C(yellow)%h %C(red)%ad %C(blue)%an%C(green)%d %C(reset)%s' --date=short --graph
	lola = log --pretty=format:'%C(yellow)%h %C(red)%ad %C(blue)%an%C(green)%d %C(reset)%s' --date=short --graph --all
	ls = ls-files
	tags = tag -l -n10

 	# Show files ignored by git:
	ign = ls-files -o -i --exclude-standard

[color]
	ui = auto
[core]
	editor = vim
[pull]
	rebase = true
[user]
	name = Pandemonium1986
	email = 15736601+Pandemonium1986@users.noreply.github.com
